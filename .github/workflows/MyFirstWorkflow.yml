name: My First Workflow

on: 
  push:
    branches: 
      - main
  workflow_dispatch:

jobs:
  MyFirstJob:
    runs-on: ubuntu
    
    steps:
      - run: echo "👋 Hello World!"
      - name: Checkout
        uses: actions/checkout@v3.5.2 
      - run: tree
